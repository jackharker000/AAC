OPENAI_API_KEY=sk-...

# Optional — if not set, the app uses your Mac's built-in voice instead
# ELEVENLABS_API_KEY=...
# ELEVENLABS_VOICE_ID=...   ← also optional; app auto-picks your first voice if not set

DATABASE_URL=sqlite+aiosqlite:///./aac.db
APP_PORT=8000
APP_HOST=127.0.0.1
